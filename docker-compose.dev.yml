version: "3.9"

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      APP_ENV: development
      BOT_POLLING_ENABLED: "True"
      BOT_WEBHOOK_ENABLED: "False"
    volumes:
      - ./app:/app/app
      - ./tests:/app/tests
    command: ["python", "-m", "app.main"]

